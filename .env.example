# DriftQ-Core image to run (pin to a known-good tag for stability)
DRIFTQ_IMAGE=ghcr.io/driftq-org/driftq-core:latest

# Demo configuration
DEMO_INDEX_NAME=demo
DEMO_DATASET=sample
EMBED_DIM=16

# Default failure behavior for the scripted demo:
# - first run fails at embed, then replay succeeds
DEMO_FAIL_STEP=embed
DEMO_FAIL_MODE=once
